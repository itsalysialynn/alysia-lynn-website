type Theme = {
  backgroundColor: string;
  color: string;
  hyperlink: {
    color: string;
    active: string;
    hover: string;
  };
  nav: {
    backgroundColor: string;
  };
  section: {
    borderColor: string;
  };
  card: {
    backgroundColor: string;
  };
};

export default Theme;
