type Theme = {
  backgroundColor: string;
  card: {
    backgroundColor: string;
  };
  color: string;
  hyperlink: {
    color: string;
  };
  nav: {
    backgroundColor: string;
  };
  section: {
    borderColor: string;
  };
};

export default Theme;
